#!/bin/sh
set -eu

# Each script runs only when the package defines it, so the same hook works
# in every repository regardless of which parts of the toolchain it uses.
npm run lint --if-present
npm run test --if-present
npm run typecheck --if-present
npm run build --if-present
npm run docs --if-present
